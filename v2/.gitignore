node_modules/
.next/
.env
.env.local
drizzle/meta/
*.log

# เอกสารตัวอย่างสำหรับทดสอบ เก็บไว้ในเครื่องเท่านั้น ไม่ต้องขึ้น git
test/

# ชุดทดสอบตัวอ่าน AN/BL — เป็นข้อความจากเอกสารจริงของลูกค้า
# repo นี้เป็น public จึงไม่เก็บขึ้นไป เก็บไว้ในเครื่องแต่ละคนแทน
# วิธีสร้างใหม่ดูใน docs/PARSER-ACCURACY.md
tests/fixtures/
